<form method="post" action="{{ path('app_offrecovoiturage_delete', {'id': offrecovoiturage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offrecovoiturage.id) }}">
    <button class="btn">Delete</button>
</form>
