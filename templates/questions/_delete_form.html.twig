
<form method="post" action="{{ path('app_questions_delete', {'questionId': question.questionId, 'sondageId': sondageId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.questionId) }}">
    <button class="btn"style="background-color: #FF5733;">Delete</button>
</form>
