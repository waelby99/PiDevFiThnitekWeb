<form method="post" action="{{ path('app_r_ponses_delete', {'réponsesId': r_ponse.réponsesId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r_ponse.réponsesId) }}">
    <button class="btn">Delete</button>
</form>
